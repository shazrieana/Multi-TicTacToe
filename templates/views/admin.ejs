<!-- filepath: /c:/Users/User/Desktop/Multi TicTacToe/templates/views/admin.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link href="https://fonts.googleapis.com/css2?family=Tilt+Warp&display=swap" rel="stylesheet">
    <style>
        * { margin: 0; padding: 0; }
        body { background-color: rgb(255, 255, 255); display: grid; place-items: center; font-family: 'Tilt Warp', cursive; }
        h1 { margin: 50px 0 50px 0; font-size: 5rem; color: rgb(32, 183, 93); -webkit-text-stroke: .5px black; }
        form { display: flex; flex-direction: column; align-items: center; }
        input { margin-bottom: 20px; padding: 5px; font-size: 1.2rem; }
        button { font-size: 1.2rem; margin-bottom: 10px; }
    </style>
</head>
<body>
    <h1>Admin Dashboard</h1>
    <p>Welcome, Admin</p>
    <!-- Add admin-specific content here -->
    <div class="container">
        <!-- User Management Section -->
        <div class="section">
            <h2>User Management</h2>
            <br>
            <button onclick="location.href='/api/users'">View All Users</button>
            <br>
            <button onclick="location.href='/api/users/new'">Create New User</button>
            <br>
            <!-- Add more user management functionalities as needed -->
        </div>

        <!-- Statistics Section -->
        <div class="section">
            <h2>Statistics</h2>
            <br>
            <p>Total Users: <!-- Add dynamic data here --></p>
            <br>
            <p>Active Users: <!-- Add dynamic data here --></p>
            <br>
            <!-- Add more statistics as needed -->
        </div>

        <!-- Settings Section -->
        <div class="section">
            <h2>Settings</h2>
            <br>
            <button onclick="location.href='/settings'">Manage Settings</button>
            <br>
            <!-- Add more settings functionalities as needed -->
        </div>
        <!-- Logs Section -->
        <div class="section">
            <h2>Logs</h2>
            <br>
            <button onclick="location.href='/logs'">View Logs</button>
            <!-- Add more logs functionalities as needed -->
            
        </div>
        <br>
         <!-- Logout Button -->
         <div class="section">
            <button onclick="location.href='/logout'">Logout</button>
        </div>
    </div>
</body>
</html>